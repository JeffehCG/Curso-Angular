//Configurações de conexão com o backend
import {environment} from '../environments/environment'

export const MEAT_API = environment.api //Pegando o rost da api, dependendo do ambiente(produção ou desenvolvimento)